import React from 'react';
import styles from './Usercard.module.css';

function Usercard({id, name, picture, access, assignment}) {
    return (
        <div>
            
        </div>
    );
}

export default Usercard;